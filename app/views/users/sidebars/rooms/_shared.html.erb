<%= link_to_room room,
      id: dom_id(room, :list), data: { sorted_list_name: room.name },
      class: [ "nav__room-btn", "unread": local_assigns[:unread], "nav__room-btn--current": (local_assigns[:current_room] && local_assigns[:current_room].id == room.id) ] do %>
  <%= room.name %>
<% end %>
